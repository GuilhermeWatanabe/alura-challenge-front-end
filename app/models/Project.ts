export class Project {
    constructor(
        private name: string,
        private description: string,
        private language: string,
        private color: string,
        private code: string
    ) {}
}